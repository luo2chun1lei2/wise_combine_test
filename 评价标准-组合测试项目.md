# 组合测试项目专项评价标准

本标准从《评价标准.md》派生，适用于“根据状态、函数、参数和关系生成并执行组合测试”的软件项目。它不是通用软件项目标准的替代品：业务价值、项目管理和流程治理仍按通用标准评价；本文件只细化组合测试产品的行为、模型、执行器和证据。若项目使用 AI 编排、保存供 AI 继续使用的资料，或提供 RAG/代码索引/结构化 AI 接口，启用《评价标准-AI附件.md》，并在本专项报告中增加 AI 补充评价。

## 1. 评价目标和标签

本专项评审冻结以下目标标签：

| 标签 | 组合测试中的含义 |
|---|---|
| `[CT-表达能力]` | 能描述状态、函数、参数、返回值、资源、关系、约束和执行配置 |
| `[CT-生成正确性]` | 只生成满足模型约束的流程，同时覆盖应测试的合法/非法组合 |
| `[CT-执行可靠性]` | 正确执行流程，识别返回值、状态、输出、超时、崩溃和协议错误 |
| `[CT-诊断复现]` | 失败报告明确，输入、seed、环境和 trace 足够重现 |
| `[CT-隔离安全]` | 被测代码、adapter、文件、进程和资源边界受到控制 |
| `[CT-测试完备性]` | 工具自身和被测模型均有正常、边界、异常和故障注入验证 |
| `[CT-扩展维护]` | 增加函数、状态、约束、执行模式和新需求时改造可控 |
| `[CT-可观测性]` | 覆盖、耗时、资源、流程和失败原因可量化和审计 |

每个检查项必须同时记录专项标签和通用标签。例如“缺少 producer 参数必须拒绝”标记为 `[CT-表达能力] [CT-生成正确性] [可靠性]`。

评审前把每项能力标为“核心契约”“已声明的可选扩展”或“明确非目标”。核心契约失败可阻断阶段门；可选扩展只在项目宣称支持、或本次评审明确纳入时扣分；明确非目标不得伪装成缺陷，但必须检查是否与用户目标冲突。空流程、子集、重复非自环、负向生成、guard/mutex/parallel/count/value、随机算法和多对象交互都要按此规则归类，不能仅因专项标准列举过就判定所有项目缺陷。

## 2. 产品层检查顺序

### 2.1 模型与 DSL/API

- 能否声明状态、初始/终止状态、迁移和状态层级；
- 能否声明函数参数、返回值、类型、资源生命周期和 producer-consumer 绑定；
- 能否声明 before/after、guard、mutex、parallel、count/value/state 等约束；
- 缺少参数、未知引用、类型不匹配、循环关系、重复定义和非法值是否拒绝；
- 每个声明的必需参数是否有且仅有一个 producer、常量或明确的外部输入来源；来源缺失必须在模型阶段拒绝；
- 模型版本、兼容性、错误位置和诊断是否清楚；
- 文档声明的字段是否真正影响后续生成和执行。

### 2.2 流程生成器

- 是否生成空流程、子集、重复调用、状态循环和多对象组合（在模型允许时）；
- 是否只输出满足所有约束的合法流程，并能生成明确的负向流程；
- before、参数依赖、资源状态、guard、mutex、parallel、计数和值约束是否真正参与生成；
- max steps/cases、seed、随机性、DFS/BFS/随机/覆盖算法等限制是否生效；
- 生成是否可终止、可扩展，并报告截断、死路和未覆盖原因；
- 相同输入和 seed 是否可复现，不同 seed 是否在声明随机时产生有意义差异。

### 2.3 执行器与适配层

- direct、adapter、动态库、harness、standalone 等模式是否遵守同一模型语义；
- 参数、返回值、状态和输出是否真实传递，而非只写入注释或固定调用零参函数；
- adapter 协议是否按完整语法解析，能区分 ok、mismatch、error、malformed、未知字段和额外输出；
- 每步/每流程超时、取消、进程组、崩溃、非零退出、SIGPIPE 和输出上限是否有界；
- 有状态被测对象是跨步骤持久化、快照恢复还是明确要求外部持久化，契约是否清楚；
- 失败是否回滚状态，成功状态是否只在断言通过后提交。
- 流程失败、超时、崩溃、协议错误、生成截断和关键报告写入失败是否转化为文档化的非零命令退出码；CI/阶段门不得忽略这些失败；
- 超时是否按进程组清理子进程和后代，避免残留任务污染后续场景。

### 2.4 报告、复现和观测

- 每条流程是否有稳定 ID、模型/配置/seed/版本摘要；
- 是否报告 expected/actual、失败步骤、状态、调用参数、stderr/stdout、退出码和信号；
- 是否支持 trace/replay、篡改检测、报告哈希和环境记录；
- 覆盖率、约束覆盖、状态/迁移/函数/关系覆盖的分母是否定义正确；
- 测量数据是否区分墙钟、CPU、内存、流程数、失败数和截断数；
- 报告和日志写入失败是否显式暴露。
- 失败报告本身写不出、目录为普通文件、权限不足或磁盘错误时，工具是否稳定报错而非异常终止或静默成功。

### 2.5 组合测试产品自身的测试

至少包含：解析和 schema 负例；参数/类型/关系约束；已声明的空、子集、重复和循环流程能力；determinism/seed；adapter 合法与恶意协议；超时、崩溃、输出上限、资源泄漏和报告失败；多模式一致性；复杂多函数有状态 fixture；sanitizer/fuzz/replay/性能验证。测试脚本不得吞掉产品失败。对未声明的扩展只记录“未验证/明确非目标”，不强行降低核心分数。

## 3. 组合测试项目的工程资料

### 3.1 需求

需求应明确：支持的模型语义、生成范围、执行模式、错误分类、隔离边界、可复现性、规模预算、性能目标和不支持的能力。每条需求都有验收 oracle 和唯一编号。

### 3.2 设计

设计应说明 parser/IR、验证器、生成器、约束求解、执行器、adapter 协议、状态提交/回滚、报告和扩展接口之间的边界。必须解释为什么选择某种生成策略、随机/确定性策略、进程隔离策略和协议格式，并列出替代方案、风险和降级行为。

### 3.3 测试设计

测试矩阵至少覆盖：模型语义 × 生成策略 × 执行模式 × 故障类型 × 报告/复现路径。组合空间过大时，说明抽样、pairwise/t-way、覆盖目标和未覆盖风险，不能只报告行覆盖率。

### 3.4 项目管理和流程

任务应按 parser、模型验证、生成、执行、报告、CLI、fixture、文档、性能和安全等可验收组件拆分；每项有依赖、出口证据和风险。重大 DSL/API 变更必须更新需求、设计、fixture、回归测试和兼容性说明。

## 4. 统一隐藏问题 oracle

评审前冻结一个与具体 DSL 无关的业务模型：`Idle -> Ready -> Closed`，另有可重复 `reset`；`open() -> token`、`consume(token)`、`reset()`、`close()`；`open before consume`，且 token 必须真实绑定。分别启用以下缺陷：

1. 缺少必需参数；
2. 类型或 producer-consumer 绑定错误；
3. 非自环状态边需要重走；
4. 子集、空流程或重复调用被错误排除；
5. guard、mutex、count/value 约束被忽略；
6. seed、max cases/steps 或生成策略选项失效；
7. direct/adapter/harness/standalone 参数或状态语义不一致；
8. 合法 JSON 的空格、字段顺序和转义，及 malformed/unknown status；
9. 超时、崩溃、部分输出、SIGPIPE、报告目录失败；
10. replay 输入篡改、版本不匹配或数值尾随字符。

保留无缺陷 fixture 作对照，每次只启用一个缺陷。分别记录：模型是否能表达、生成器是否能覆盖触发流程、执行器是否正确判错、报告是否可复现。输出缺陷发现率、正常用例误报率、无法表达数和未执行数。

供应触发序列的执行实验只能证明执行器对给定流程的判错能力，不能证明生成器会自动发现该流程；需要另列生成 probe。每个 probe 标注所测对象是 state cycle、relation DAG、subset、repeat、约束过滤还是执行器模式一致性，禁止把不同对象的结果互相替代。

## 5. 组合测试阶段门

| 阶段 | 必须回答的问题 | 进入下一阶段的最低条件 |
|---|---|---|
| 需求 | 模型语义、范围、规模和不支持边界是否明确 | 需求编号、oracle、风险和验收标准冻结 |
| 设计 | 生成、执行、隔离、报告边界是否闭合 | 需求覆盖矩阵、接口契约、失败路径和测试矩阵通过 |
| 编码 | 实现是否遵守 DSL/API 和设计 | 核心 parser/validator/generator/runner 回归通过，无未缓解 High |
| 测试 | 是否发现统一隐藏问题并能复现 | 正常/异常/边界/安全测试和报告证据齐全 |
| 发布 | 工具和 fixture 是否可安装、运行和诊断 | 干净构建、版本兼容、回滚/文档/限制说明通过 |
| 维护 | 需求或 DSL 变更影响是否受控 | 变更分析、回归测试、文档同步和经验任务完成 |

## 6. 专项评分

按 0–10 分评价：

1. 模型表达和契约（`[CT-表达能力] [功能完整性]`）；
2. 组合生成正确性和覆盖（`[CT-生成正确性] [CT-可观测性]`）；
3. 执行可靠性、隔离与安全（`[CT-执行可靠性] [CT-隔离安全] [可靠性]`）；
4. 报告、诊断和复现（`[CT-诊断复现] [CT-可观测性] [可测试性]`）；
5. 工具自身测试完备性（`[CT-测试完备性] [可测试性]`）；
6. 设计、文档和后续维护（`[CT-扩展维护] [可维护性]`）；
7. 项目计划、风险和流程治理（`[交付性] [效率] [可维护性]`）。

默认平均七项；评审前可冻结其他权重。通用标准的 9–10、7–8、4–6、0–3 锚点、High 硬门槛、证据覆盖率和阶段门规则全部适用。专项标准不能用高行覆盖率掩盖生成语义漏测或错误判定。

## 7. 专项报告最低结构

除通用报告字段外，必须列出：模型/DSL 版本；状态、函数、参数和约束矩阵；生成策略和限制；执行模式对齐表；统一 oracle 与每个缺陷结果；流程/状态/关系/约束覆盖定义；adapter/fixture/故障注入证据；未支持和未验证语义；阶段门结论和修复优先级。

## 8. AI 补充评价（启用附件时）

除《评价标准-AI附件.md》的通用项目检查外，组合测试项目重点检查：

- `[AI-质量提升]` AI 是否帮助发现模型约束遗漏、生成器边界、adapter 协议或测试 oracle 缺陷，并有可复现证据；
- `[AI-可延续性]` 是否保存 DSL/API、状态/关系模型、技术调查、ADR、fixture、失败报告和回归命令，使 AI 能继续定位和修改；
- `[AI-接口能力]` 是否提供稳定的 JSON/CLI、模型校验、生成、运行、报告、trace/replay、代码索引或 RAG 查询接口，并返回版本、来源和权限信息；
- `[AI-可控性] [AI-安全性]` AI 生成的模型、adapter、测试和代码是否经人工审查、自动测试、许可证/安全扫描，是否隔离不可信执行和凭证；
- `[AI-成本] [AI-持续性]` AI 调用、人工复核、状态桥、fixture 适配和失败定位成本是否记录，模型/供应商不可用时是否有替代和复现路径。

四个方案的 Q1–Q6 适配资产应标明哪些是产品原生能力、哪些是评审 adapter 的别名/状态桥；不能把外部 adapter 产生的模型展开和断言当作项目本身已有的 AI 接口能力。AI 贡献还应区分“帮助执行已给流程”和“帮助生成器发现流程”。

启用附件时，专项七项软件分仍为 `S_software`，AI 五项分为 `S_AI`；默认最终分数为 `0.85 × S_software + 0.15 × S_AI`。AI 分不能抵消模型参数契约、执行隔离、错误退出码或其他软件 High 问题。未启用时 AI 维度记“不适用”，不按 0 分计。
