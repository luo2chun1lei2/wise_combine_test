# 软件项目 AI 评价附件

版本：1.0（2026-09-12）。本文是《评价标准.md》的可选附件，不改变软件本身的质量门槛。

## 1. 何时启用

在以下任一条件成立时启用本附件，并在评审报告中记录启用原因：

- AI 参与了需求、调查、设计、编码、测试、发布或维护；
- 项目把 AI 作为产品能力、开发工具、运维工具或交付目标；
- 项目要求未来由 AI 或 AI 辅助人员继续开发、排障或查询资料；
- 项目提供 RAG、知识库、代码索引、结构化 CLI/API 或其他 AI 专用接口；
- 本次评审需要比较不同 AI 编排方式的效率、质量或可延续性。

如果项目完全没有 AI 参与、AI 继续开发不在目标内、也没有 AI 接口，可以标记“AI 附件不适用”，不因未使用 AI 扣分。不能用不启用附件来掩盖项目实际使用的 AI 风险。

附件可以按阶段启用：需求阶段先评 AI 调查和资料；编码阶段评 AI 产出控制；测试阶段评 AI 测试质量；维护阶段评 AI 能否继续使用项目资料。迭代结束时汇总本迭代 AI 净收益和遗留风险。

## 2. AI 总目标标签

每个 AI 检查项至少关联一个标签：

- `[AI-效率提升]`：减少时间、重复工作、返工或定位成本；
- `[AI-质量提升]`：提高需求、设计、代码、测试或运维质量；
- `[AI-人工协同]`：人工能理解、审查、否决和接管 AI 产出；
- `[AI-可延续性]`：资料能支持未来 AI 和人工继续开发；
- `[AI-接口能力]`：AI 能通过稳定、结构化、可授权的接口使用系统和资料；
- `[AI-可控性]`：模型、提示、输入、输出、版本和批准点可追踪；
- `[AI-安全性]`：防止提示注入、凭证泄漏、越权、不可信代码和敏感数据扩散；
- `[AI-成本]`：模型、调用、RAG、人工审查和返工成本可接受；
- `[AI-持续性]`：供应商、模型、网络或版本变化时仍有替代和复现路径。

## 3. 评价维度

### 3.1 AI 贡献效果

记录 AI 参与的阶段、任务、输入资料、模型和版本，以及产出被采纳、修改或拒绝的原因。用同一任务的人工或历史基线比较：

- 完成时间、人工时间、返工和调用成本；
- 需求遗漏、设计缺陷、代码缺陷、测试缺陷和误报/漏报；
- 文档完整性、问题定位时间和交付稳定性。

只记录“用了多少次 AI”不能证明收益。AI 生成代码但增加返工，或 AI 生成测试但没有真实缺陷检出，不得仅因调用次数加分。

### 3.2 AI 可继续使用的项目资料

检查调查记录、需求、设计决策、被否决方案、API/数据/状态模型、构建测试部署命令、故障排查、已知问题和技术债：

- 是否有稳定路径、格式、索引、标签、版本和更新时间；
- 是否能从资料追溯结论依据和适用版本；
- 是否同时服务人工维护和 AI 继续开发；
- 是否通过实际后续任务证明资料可用。

资料多不等于资料可用。应区分“存在”“结构化可检索”“AI 正确使用”“后续任务实际复用”。

### 3.3 AI 专用接口和优化接口

检查稳定 CLI/JSON、OpenAPI/JSON Schema、代码搜索/符号索引、依赖图、测试运行、日志查询、任务/缺陷/变更影响查询和 RAG 服务：

- 返回是否结构化、带来源、版本、权限、更新时间和错误状态；
- 是否支持增量上下文，避免每次传入整个仓库；
- RAG 是否能找到正确版本，是否隔离无权限或过时内容；
- 查询失败、资料缺失和低置信度是否明确暴露；
- 接口升级是否有兼容和回归策略。

### 3.4 AI 质量控制与安全

检查模型名称/版本、时间、主要提示、输入资料范围、输出、人工审查、批准和回滚记录；验证代码审查、测试、许可证、安全扫描、提示注入、凭证和不可信执行防护。模型或供应商变化后应能重新验证关键结果。

### 3.5 AI 成本与持续性

记录 token/API/模型/RAG/向量库/推理环境、人工审查和返工成本，及对单一供应商、联网服务或凭证的依赖。检查限流、模型不可用、版本升级、离线复现和替代方案。

## 4. AI 证据表

| 项目编号 | AI 标签 | 阶段和任务 | 输入资料 | 模型/接口版本 | 人工控制点 | 质量/效率证据 | 成本/风险 | 结论 |
|---|---|---|---|---|---|---|---|---|
| AI-001 | `[AI-可延续性] [可维护性]` | 设计调查 | 调查记录、ADR | 填写版本 | 设计评审 | 后续任务正确复用 | 过时风险 | 填写结论 |

## 5. AI 分数和硬门槛

AI 评价是可选的第七个维度，默认由五项平均：贡献效果、可延续资料、接口能力、质量与安全、成本与持续性，满分 10 分。

不启用本附件时，最终分数为通用/专项软件维度的加权平均；AI 维度记“不适用”，不作为 0 分。启用后，默认最终分数为：

`S_final = 0.85 × S_software + 0.15 × S_AI`

项目可在评审前冻结其他权重。AI 分不能抵消软件质量、可靠性、安全或阶段门的 High 问题。AI 资料、接口或控制点被项目明确承诺为核心能力而未实现时，可以阻断对应阶段；仅因为项目没有提供可选 RAG 不得阻断普通软件发布。

没有模型/版本/人工控制/质量证据时，AI 贡献效果不得超过 5 分；没有可复用资料时，AI 可延续性不得超过 4 分；接口返回无来源、权限和版本信息时，接口能力不得超过 5 分；存在未控制的凭证泄漏、越权或不可信代码执行时，AI 安全为 High。

## 6. 阶段评审重点

| 阶段 | AI 重点 |
|---|---|
| 需求 | AI 调查是否可追溯，需求来源和人工确认是否保存 |
| 设计 | AI 是否比较方案，设计决策、调查和被否决方案是否保存 |
| 编码 | 生成代码的审查、测试、许可证、安全和回滚 |
| 测试 | 生成测试的有效性、误报/漏报和缺陷检出 |
| 发布运维 | AI 查询接口、运行手册、日志和故障资料是否可用 |
| 维护变更 | AI 能否基于资料完成影响分析、修改和回归 |
| 迭代结束 | 本迭代的净收益、成本、错误、资料沉淀和供应商风险 |

## 7. 结论和限制

AI 评价只说明本次项目中可观察的 AI 使用效果和延续能力，不能据此证明某个模型、厂商或编排工具普遍更优。模型、人工决策、预算和迭代次数未受控时，报告必须声明因果结论受限。
