# 软件项目评审标准与方法

版本：2.2（2026-09-12）。本次修订补充阶段适用性、审查覆盖、测量有效性、统一失败门禁、来源完整性和专项标准反馈规则；历史报告不自动追溯重算。

## 1. 目的和适用范围

本文用于评审软件项目、产品方案、AI 辅助开发方案及其阶段性交付物，目标是发现影响产品质量、项目交付和后续维护的问题，并决定能否进入下一阶段。

本文适用于瀑布式、敏捷式、迭代式和 AI 编排的软件项目。可评审完整项目、某个阶段、某次迭代、重大变更或专项风险。组合测试工具只是可被评审的软件类型之一，不是本文默认对象。

AI 使用、AI 继续开发资料或 AI 专用接口属于本项目目标时，启用配套附件《评价标准-AI附件.md》。附件是可选的阶段性评审维度：需求阶段可只评调查资料，编码/测试阶段评产出控制，维护阶段评资料和接口的延续性。项目完全不使用 AI 且没有 AI 延续目标时，在报告中写明“AI 附件不适用”，不因未使用 AI 扣分。

评审不是项目结束后的单次验收，应在需求、设计、编码、测试、发布和维护阶段结束时重复执行；迭代项目至少每个迭代执行一次适用范围内的全面评审。

## 2. 评审总目标和标签

评审开始前声明总目标、优先级、权重和通过条件。每个具体检查项必须关联一个或多个标签，形成“总目标 → 检查项 → 证据 → 结论 → 改进”的追踪链。检查项还必须标为“核心契约”“已声明的可选扩展”或“明确非目标”，避免把未承诺能力当成普遍缺陷。

| 标签 | 总目标 |
|---|---|
| `[业务价值]` | 产品解决真实问题，范围、价值和验收结果清楚 |
| `[功能完整性]` | 约定功能、接口和业务规则得到实现 |
| `[质量]` | 正确性、可用性、兼容性和用户体验达到要求 |
| `[可靠性]` | 错误、异常和边界场景下结果正确且可诊断 |
| `[稳定性]` | 重复运行、压力、依赖异常和资源受限时行为可预测、有界 |
| `[安全性]` | 输入、权限、数据、依赖和部署受到适当保护 |
| `[可维护性]` | 代码、文档、测试和决策可理解、可追踪、易修改 |
| `[可扩展性]` | 增加功能、规模、平台或需求时改造成本可控 |
| `[可测试性]` | 行为可观察、结果可判定、故障可注入、问题可复现 |
| `[交付性]` | 在约定时间、资源和质量约束下能够交付 |
| `[效率]` | 构建、运行、开发、AI 调用和维护成本合理 |
| `[合规性]` | 满足适用的法律、许可证、行业和组织规范 |

可以增加领域标签，但评审前必须冻结标签含义、权重和验收口径。标签说明评价目的，不等于评分维度。例如“产品应该记录每一步设计决策”应标记为 `[可维护性] [可扩展性]`。

## 3. 评审对象的层次和顺序

评审按以下顺序组织证据。后一个层次不能替代前一个层次；管理记录完整不能弥补产品不可靠，文档齐全也不能证明代码正确。

### 3.1 第一层：产品和交付物

检查产品、服务、库、脚本、模型、配置、部署包等交付物：

- 是否满足业务目标、范围和验收标准；
- 功能、性能、可用性、兼容性、可靠性和安全性是否达标；
- 正常、错误、边界、恢复和升级场景是否可用；
- 已知缺陷、限制和技术债是否被识别并接受；
- 不同运行模式、平台和部署环境的行为是否一致。

当前阶段没有可运行产品时，该层标记为“暂不适用”，不能根据文档推定产品合格。

### 3.2 第二层：产品工程资料

检查需求、范围、验收标准、架构、接口、数据模型、技术选型、测试设计、部署和运维资料：

- 需求是否完整、明确、可验证，且覆盖业务和非功能目标；
- 需求之间是否冲突，优先级和不确定项是否明确；
- 设计是否覆盖需求，边界、失败路径、约束和接口契约是否清楚；
- 技术选型是否比较替代方案、成本、风险和长期影响；
- 测试设计是否能验证需求、风险和关键设计决策；
- 文档是否与代码、配置和实际部署一致，是否缺失或过时。

### 3.3 第三层：项目管理资料

检查项目如何把目标和设计转化为工作：

- 任务是否拆分到可交付、可验收的粒度；
- 依赖、里程碑、工期、人力、AI 调用、基础设施和成本是否有依据；
- 责任人、完成定义、验收证据和沟通机制是否明确；
- 风险、假设、外部依赖和应对措施是否登记并更新；
- 计划偏差、资源消耗和未完成事项是否及时反映。

### 3.4 第四层：流程治理和变更管控

检查项目是否持续受控：

- 阶段入口、出口和审批标准是否明确；
- 版本、配置、分支、构建物和环境是否可追踪；
- 需求、设计、代码和测试变更是否进行影响分析；
- 缺陷是否有优先级、责任人、修复、回归和关闭证据；
- 发布、回滚、监控、应急和运维交接是否准备充分；
- 评审意见、例外批准和经验教训是否形成闭环。

## 4. 按生命周期阶段评审

每次评审先写明当前阶段、范围、已产生的交付物和暂不适用内容。

| 阶段 | 必查内容 | 出口问题 |
|---|---|---|
| 需求收集 | 目标、用户、范围、场景、约束、优先级、验收标准、风险 | 能否开始方案设计 |
| 方案与设计 | 需求覆盖矩阵、架构、接口、数据/状态、选型、失败路径、测试策略 | 设计是否可实现、可验证、风险可接受 |
| 编码与实现 | 可运行增量、代码质量、静态检查、单元测试、接口契约、已知缺陷 | 能否进入系统测试 |
| 测试与验收 | 功能、异常、边界、性能、安全、兼容性、回归、缺陷和发布风险 | 是否满足发布条件 |
| 发布与运维 | 部署、配置、监控、告警、回滚、权限、备份、运维手册 | 能否安全发布和持续运行 |
| 维护与变更 | 影响、回归、兼容性、文档同步、缺陷复盘、维护成本 | 能否合并、发布或继续扩展 |
| 迭代结束 | 迭代范围全面检查、遗留项、指标和下一迭代输入 | 迭代是否完成、遗留如何处理 |

“暂不适用”必须写明原因和预计评审时间；它不等于通过。阶段评审复用目标标签、需求编号和问题编号，以便跨阶段追踪。

迭代结束时只评审本迭代承诺范围，未来功能标记为“范围外”。重大需求或架构变更、供应链变更、生产事故或高风险缺陷修复后，应立即增加专项评审。

## 5. 评审方法

### 5.1 冻结共同验收依据

评审前独立编写并冻结验收依据（test oracle）：输入、预期行为、质量阈值、错误处理、退出条件和不可接受结果。验收依据不能从被评审方案的宣传材料或测试结果反向推导。不同实现可以用适配层映射相同业务含义，但适配层不能补写产品断言、掩盖失败或实现产品缺失的核心能力。

故障注入适用于已有可执行实现、且值得验证故障处理或测试检出能力的阶段。适用时保留无缺陷版本作对照，每次只启用一个预置缺陷，避免故障互相掩盖。分别统计“是否覆盖触发场景”和“面对触发场景是否正确判定”。需求和设计阶段用需求走查、实例推演或原型验证，不强求运行产品。

薄适配层可以转换协议、传递原始数据；测试标准允许时也可按模型建立断言。应禁止的是：适配层自行判断预置缺陷编号、补造测试结果、替代产品本应提供的断言或偷偷实现缺失的生成能力。额外状态持久化、包装函数和模型展开必须披露，分别报告原生能力与接入后的能力。

### 5.2 独立审查和交叉复核

每个方案或关键阶段至少由一名独立审查者检查；比较多个方案时使用相同清单、输入、预算和证据要求。每个方案至少启动一个独立 subagent。汇总者负责核对结论、统一严重程度和评分，收齐各方结论后再排名。

### 5.3 风险驱动验证

优先检查业务关键路径、复杂依赖、历史缺陷、外部接口、权限边界、高变更区域和最可能失败的场景。加入错误输入、边界值、并发、恢复、升级、降级和资源耗尽场景。对 AI 生成代码，增加生成内容审查、依赖许可证、提示注入和凭证泄漏检查。

### 5.4 变更维护试验

已有实现且评价维护能力时，至少模拟一次小型变更：增加字段或接口、添加状态、调整约束、修复缺陷或支持新平台。记录受影响文件和测试、回归结果、文档同步、人工干预、AI 调用、耗时和成本。各方案使用同一变更要求、验收标准和时间预算，在独立副本进行；需求和设计阶段可做变更影响推演，标记为“未实际验证代码变更”。

## 6. 证据和可复现性

证据优先级：可重复命令和自动化测试；产品运行结果、监控和验收记录；源码、接口、配置和构建文件；需求、设计、决策、任务和审查文档；口头说明或未执行计划。

每项检查使用：**已实现并验证、已实现但部分验证、明确不支持、需求要求但未实现、尚未验证、不适用于当前阶段**之一。明确不支持只有在不违反共同目标且边界清楚时才可接受；未实现和未验证不能按通过处理。

记录版本或 commit、操作系统、编译器/运行时、依赖版本、完整命令、输入、输出、日志、文件哈希和未提交改动。关键场景至少重复两次；性能至少测量三次并报告中位数和范围。历史证据标注对应版本，不能直接作为当前版本通过证明。

| 项目编号 | 总目标标签 | 预期行为/阈值 | 支持边界 | 证据状态 | 实际结果 | 版本、命令和日志 | 风险及评分影响 |
|---|---|---|---|---|---|---|---|
| R-001 | `[可靠性]` | 非法输入应拒绝并返回明确错误 | 当前版本支持 JSON 输入 | 已验证 | 拒绝输入且诊断准确 | 填写可复现信息 | 通过，无扣分 |

适用范围、支持状态、验证结果分开记录。证据结果使用“通过 / 失败 / 部分 / 未验证”；当前阶段到期却缺失的工作算缺口，不能改写为“不适用”。文档/管理检查的证据可以是仓库资料、记录之间的对照和检索范围，不要求以运行命令证明设计合理性。

## 7. 缺陷、风险和验证指标

每个问题写明触发条件、实际结果、预期结果、影响范围、责任人、补救措施、期限和复核条件。

- **High**：核心目标失效、错误结果被判为成功、可能挂死、越权、数据泄漏或破坏隔离；
- **Medium**：重要边界或扩展能力不完整，常规场景仍可用；
- **Low**：诊断、文档、体验或非核心效率问题。

统一测试场景报告：缺陷发现率 = 正确发现的唯一缺陷数 / 冻结的有效缺陷总数；正常用例误报率 = 被错误判为失败的正常用例数 / 执行的正常用例总数；另列漏报、无法表达、无法执行和未验证数量。同一缺陷多次触发只计一次；工具崩溃不算成功发现产品缺陷。

证据覆盖率 = `已有充分证据判为通过或失败的检查项数 / 当前阶段适用检查项总数`。分母使用评审前冻结的唯一编号清单；部分验证、仅文档声明和未验证不计入。每层分别报告原始计数，通过率另列；已证明的失败计入证据覆盖，但不计入通过率。经说明的不适用项不计入分母。

代码覆盖率是辅助信息，不能替代需求、风险或验收依据覆盖。报告测量对象、编译选项、测试入口、分母和工具限制；分清行覆盖、分支执行与分支命中。多进程、异常退出或未插桩组件可能漏收数据，不能在未验证采集有效性时把低覆盖直接解释为产品没有被测试。

## 8. 评分和阶段门

### 8.1 评分维度

适用时按 0–10 分评价：

1. 产品质量与目标匹配（`[业务价值] [功能完整性] [质量]`）；
2. 可靠性、稳定性与安全（`[可靠性] [稳定性] [安全性]`）；
3. 工程设计和测试完备（`[可测试性] [功能完整性] [可维护性]`）；
4. 可维护性和可扩展性（`[可维护性] [可扩展性]`）；
5. 项目计划、成本和风险（`[交付性] [效率] [稳定性]`）；
6. 流程治理和变更控制（`[可维护性] [可靠性] [合规性]`）。

评分锚点：9–10 表示目标完整且关键边界有证据；7–8 表示主流程可靠但有中等缺口或证据不完整；4–6 表示可演示但有明显偏差、边界缺陷或测试不足；0–3 表示核心目标无法稳定完成。没有证据不得默认通过，应标记未验证并限制对应分数上限。默认综合分为适用维度平均，不适用维度不记0或10分；使用权重时在适用维度中归一化。专项标准可拆分维度，但必须映射回四层对象并在评分前冻结。不同阶段的总分不可直接排名。

### 8.2 硬门槛

- 未缓解的 High 问题：不得无条件进入下一阶段或列为首选；
- 核心需求未通过：产品质量与目标匹配不得超过 6 分；
- 可靠性关键场景未验证：可靠性不得超过 6 分；
- 验收依据未独立冻结或证据覆盖率低于 60%：测试完备不得超过 6 分；
- 产品或测试断言的关键失败被吞掉，且未核对预期退出码：可靠性及相关测试评分至少各扣 1 分；仅看到 `-`、`|| true` 不足以判错，预期失败测试可以明确捕获并核对结果；
- 关键变更未做影响分析和回归：不得无条件发布。

默认政策可以在评审前统一调整，但必须记录标准版本和适用范围。

阶段门只作用于当前申请的下一步；设计阶段不会因尚无运行证据被产品测试门槛阻断。阻断发布不等于阻断为修复缺陷开展编码。未缓解的High不得通过改写分数或把问题移出范围来绕过；同一事实在同一维度不重复扣分。产品或测试断言的流程失败、超时、崩溃、截断和关键报告写入失败必须有文档化的非零退出语义，CI 和阶段门不得忽略；预期失败测试可以捕获并核对非零值，不能仅凭命令中出现 `-` 或 `|| true` 判定。

### 8.3 阶段门结论

每份评审明确以下结论之一：

- **通过，可进入下一阶段**：出口标准满足，无阻断问题；
- **有条件通过**：允许并行进入下一阶段，但列出补救任务、责任人、期限和复核条件；
- **不通过，暂缓进入下一阶段**：存在阻断问题，补救后重新评审；
- **暂缓评审**：关键输入、环境或证据缺失，补齐后再评审。

结论必须列出阻断问题、未验证事项、补救措施、重新评审条件和影响。“有条件通过”不能绕过 High 问题、核心需求缺失或关键证据缺失。若所有方案都有 High 问题，可以给相对顺序，但必须明确“无合格首选”。

## 9. 经验反馈和持续改进

评审结束回答：哪些做法有效；哪些问题本可更早发现；哪些需求、设计、编码、测试、计划或流程需要改进；工期、成本和风险估算有何偏差；下一迭代或未来项目应增删哪些检查项。

每条经验转化为改进任务、标准变更、模板更新或培训事项，指定责任人和完成时间，不能只停留在会议纪要中。

## 10. 报告和排名

报告至少包含：项目/版本/日期/阶段/范围/参与者；总目标、权重和出口标准；四层评审对象及适用范围；验收依据、输入、环境和命令；证据表、问题、风险和覆盖率；分项分数、硬门槛和调整分数；阶段门结论；阻断问题和补救任务；未验证、明确不支持和暂不适用项目；经验、改进任务和下次评审时间；标准版本和限制。启用 AI 附件时还要记录 AI 适用原因、AI 阶段/任务、模型和接口版本、人工控制点、效率/质量/成本证据及 AI 分数；不适用时记录原因。

排名先应用硬门槛，再比较调整后综合分。分差小于 0.5 分时，优先比较证据覆盖率、变更维护实测和文档一致性，再比较成本；证据无法区分时允许并列。交付效率与资源成本作为附加指标报告，包括人工时间、AI 调用、决策次数、构建/运行资源、定位步骤、外部依赖和凭证需求；缺少记录标为未知。

本标准不替代业务验收、法规或组织审批制度。标准更新必须有版本号；历史报告保留当时版本，复评时补齐新增证据后重新评分。启用 AI 附件时，软件质量分仍是主分，AI 分不能抵消软件质量、安全或阶段门的 High 问题；默认按附件规定的 85% 软件分 + 15% AI 分计算最终结果。

## 11. 从专项评审反馈到标准

区分“项目应有但没有的交付物”和“标准未覆盖的检查点”：前者登记为项目缺口，后者登记为标准改进。找不到工期、成本或风险记录时，报告检索范围和证据缺失，不虚构估算，也不简单按文档数量评价管理质量。

标准改进记录包括来源证据、原规则盲区、新规则、通用/领域归属、影响的检查项和版本。一般性方法同步修改通用标准，领域语义写入专项标准；若变化影响分数或实验结论，对所有比较对象采用相同规则补查或重算。记录增补时点，不能声称已知旧缺陷的复测是盲测。只有实际完成的实验才产生结果和发现率。

对AI编排方式的评价限于可观察到的本次交付：模型、人工决策、预算和迭代次数未受控时，不把四份产物的排名解释为编排工具本身的因果优劣。
