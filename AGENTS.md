# 组合测试 (combination test)

## 项目要求

- 尽量采用简单的技术和方法实现。
- 基于 C/C++ 代码开发，测试和构建可以采用其他语言，建议用 Makefile。
- 工作目录下的文件夹和重要文件的分布和说明放在 doc/layout.md 中。
- 每次重大修改后，都需要用户确认后，再提交代码到服务器，包括AI需要的文档。

## 对AI的要求

- 对于简单工作，不需要复杂推理之类的，比如运行测试、生成Makefile之类的工作，可以用低等级的AI模型（比如 gpt-5.6 terra 或 DeepSeek v4-flash）。
- 对于测试和评审工作，要启动子智能体或其他的Agent去完成。
- 同一个任务如果因为同一个问题三次无法完成，请退出任务，然后分析问题。

