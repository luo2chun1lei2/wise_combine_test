# Capability matrix

Last boundary review: 2026-09-14. Status is scoped to the implementation and
tests at candidate `41669164a69033a5b51383c26c638f90b2a9e52a`; independent
review is pending and this is not a release PASS. Runtime status is tracked in
[`.omx/current-status.md`](.omx/current-status.md).

| Area | Status | Product behavior | Primary code/test pointers | Evidence pointers |
| --- | --- | --- | --- | --- |
| State transition testing | Supported | Parse and validate state graphs; execute reachable transitions in graph order with step and edge coverage reporting. | `include/wct.h`, `src/wct.c`, `fixtures/state_graph.model`; `test_state_success_and_limit`, `test_state_validation_errors`, `test_state_branching_and_unreachable` in [`tests/test_api.c`](../tests/test_api.c). | `make test`; [`fixtures/state_graph.model`](../fixtures/state_graph.model); historical I15 test evidence. |
| Relation flow testing | Supported | Parse explicit call/edge relations, reject invalid DAGs, execute topological flows, and report call/edge coverage. | `fixtures/function_relations.model`; `test_relation_order_and_failure`, `test_multiple_bounded_relation_flows`, `test_relation_cycle` in [`tests/test_api.c`](../tests/test_api.c). | `make test`; CLI command in [`README.md`](../README.md); historical I15 test evidence. |
| Deterministic scheduling / queue | Supported | Deterministic graph-order state execution; unseeded relation flow 0 uses lexical tie-break and later flows use declaration-order rotation; non-zero seeds use deterministic xorshift sampling; bounded multi-flow exploration. | `test_seed_sampling_determinism`, `test_relation_lexical_tie_break`, `test_multiple_bounded_relation_flows`, `test_relation_result_binding` in [`tests/test_api.c`](../tests/test_api.c); determinism section in [`docs/api-contract.md`](../docs/api-contract.md). | I17 test/trace evidence; [`docs/oracle-matrix.md`](../docs/oracle-matrix.md). |
| Contracts | Supported | Optional call arity and argument types; explicit zero-arity contracts; producer result type; exact result assertion; `$producer` reference typing. | `docs/api-contract.md`; `test_parser_call_contract`, `test_parser_result_contract`, `test_relation_arity_and_types`, `test_relation_result_contracts` in [`tests/test_api.c`](../tests/test_api.c). | `make test` and historical I15 test evidence. |
| Process isolation | Supported | Complete CLI scenarios are fork-isolated by default; optional explicit monotonic deadline; child exit/signal/timeout metadata is reported. State scenarios use snapshot/restore or parent-managed transactional commit. | `test_isolation_timeout`, `test_timeout_range_and_zero_arity_contract`, `test_isolated_state_snapshot_commit`, `test_zero_snapshot_and_rollback_failure` in [`tests/test_api.c`](../tests/test_api.c); CLI isolation description in [`README.md`](../README.md). | I17 `sanitize.*`, `valgrind.*`, historical I15 isolation logs. |
| Trace and replay | Supported | Versioned trace with canonical model/IR/metadata and step digests; replay re-runs the referenced model and rejects malformed or tampered metadata/results. | `--trace` and `--replay` in [`tools/wct_cli.c`](../tools/wct_cli.c); trace/tamper matrix in [`tests/test_cli.sh`](../tests/test_cli.sh). | I17 `trace.*`, `replay.*`, and `tampered-replay.*`; command examples in [`README.md`](../README.md). |
| Arbitrary parameter-space search | Unsupported / out of scope | The runner explores explicitly declared graph paths and bounded flows. It does not synthesize unbounded parameter domains or exhaustive value combinations. | Scope statement in [`docs/release-readiness.md`](../docs/release-readiness.md); project brief in [`AGENTS.md`](../AGENTS.md). | No implementation or release claim. |
| Inferred relations | Unsupported / out of scope | Dependencies come from explicit relation edges or `$producer` references. The tool does not infer undeclared relationships from callback behavior. | `test_relation_reference_dependency` covers only an explicitly encoded `$produce` dependency. | No implementation or release claim. |
| Generic callback context object | Unsupported / out of scope | Callback context remains application-owned. State support requires explicit snapshot/restore/reset hooks; relation callback context is not automatically promoted to a portable generic object. | Ownership contract in [`docs/api-contract.md`](../docs/api-contract.md); transaction tests listed above. | No generic-context implementation or release claim. |
| Automatic relation callback side-effect commit | Unsupported / out of scope | A relation scenario executes in the child; there is no automatic commit of arbitrary callback context to the parent. Results must cross through the declared callback result contract. | Relation isolation and transaction behavior in [`README.md`](../README.md); `relation_snapshot` is test-local, not a product generic context API. | Historical I15 tests; no generic side-effect commit claim. |
| Controlled queue executor/oracle regression | Supported | Eighty-four clean/mutant matrix runs and three generation probes detect six supplied Queue mutants through declared result contracts. | [`tests/test_queue_oracle.sh`](../tests/test_queue_oracle.sh) and [`docs/oracle-matrix.md`](../docs/oracle-matrix.md). | I17 `test.*` and `queue-results.tsv`. |
