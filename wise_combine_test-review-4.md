# wise_combine_test 四方案第三次复评（第四轮评审）

评审标准：[评价标准.md](评价标准.md)（v2.2，2026-09-12）、[评价标准-组合测试项目.md](评价标准-组合测试项目.md)、[评价标准-AI附件.md](评价标准-AI附件.md)（v1.0，启用）
评审日期：2026-09-15；上一轮（review-3）完成于 2026-09-12。
评审对象版本、命令、日志和项目级证据：[wise_combine_test-review-4-assets](wise_combine_test-review-4-assets/)（含 snapshot.json、四个项目的 independent-review.md、汇总者交叉复核 cross-check.md）。

本轮为 review-3 之后的复评：四个项目在上轮结论后均完成了一轮补救提交（OMX 21 个提交 +7502 行、LazyCodex 89 个提交 +5112 行、Manual 11 个提交 +2081 行、Plan/Goal 16 个提交 +2171 行）。按通用标准 §5.2，由四个独立 subagent 按相同任务书分别复评，汇总者对关键承重结论做命令级交叉复核（Plan/Goal 冻结口径误报、LazyCodex 模型阶段拒参、OMX 严格解析源码、Manual 自身测试套件，共 5 项，全部复现成功），再统一严重程度与评分。

## 1. 版本快照与补救规模

| 方案 | review-3 HEAD | 本轮 HEAD | 补救提交 | 行变更 | 评审后工作区 |
|---|---|---|---:|---|---|
| OMX | 3985089 | 1faa41e | 21 | +7502/−76 | 干净 |
| LazyCodex | 52a7fa5 | b67e24f | 89 | +5112/−73 | 干净 |
| Manual | 96d639a | 7eb9ce1 | 11 | +2081/−230 | 干净 |
| Plan/Goal | e3f2e47 | eb219c6 | 16 | +2171/−254 | 干净 |

Manual 与 Plan/Goal 的补救集中在 review-3 定稿后约 80–90 分钟内完成；OMX 与 LazyCodex 持续到 09-14。四个项目均未记录补救消耗的人工时间/AI token 成本（OMX 有 goal 级聚合 token，无单次计量）。

## 2. review-3 遗留问题核验总表

详细逐项核验（含命令与 file:line 证据）见各项目独立报告。汇总：

| 方案 | review-3 问题数 | 已修复并验证 | 部分修复/按边界重分类 | 未修复 |
|---|---:|---:|---|---|
| OMX | 4（1 High/Medium 解析、重复声明、覆盖归集、状态冲突） | 4 | 0 | 0 |
| LazyCodex | 7（参数来源 BLOCK、seed、约束能力、JSON 解析、reports 错误、报告元数据、`.agents`/KB） | 5 | 2（约束语义按 ADR 0004 冻结为非目标；`.agents` 追踪矛盾未解） | 0 |
| Manual | 8（CLI 契约×2、guard、嵌套 harness、静默 true/重复声明、`\|\| true`、统一 oracle 未完成、count/value 表达） | 8（重复声明留 1 个 Low 残留：完全相同的 transition） | 0 | 0 |
| Plan/Goal | 9（退出码 High、direct/standalone 零参 High、JSON 解析 High、缺参来源、max-flows 翻倍、单 PID 超时、报告元数据、生成覆盖、`make check` 吞失败） | 9（direct/standalone 以"显式拒绝+文档化边界"方式修复，能力降级为声明边界而非隐蔽缺陷） | 0 | 0 |

review-3 列出的全部 High 问题在本轮均确认修复并以回归锁定。关键修复的独立复测：

- OMX：`schema 1x`/`contract a 0x`/篡改 `seed 0x` 全部拒绝（exit 1）；重复 `state_graph`/`relation_graph`/`schema` 声明拒绝并报告首次声明行号；fork 覆盖归集（22 个子 profile 合并）后行覆盖 78.55%/92.01%，从 18.87% 修复且 Makefile 对缺失 profile fail-closed。
- LazyCodex：删除 argument 关系后 `validate` 在模型阶段拒绝（`invalid_argument` 稳定错误码，exit 2）；seed 真实参与生成（同 seed 复现、异 seed 有差异）；adapter 输出对合法空格/字段换序/Unicode 转义接受、对重复键/尾随内容/未知 status 拒绝（exit 5）；`--reports` 指向普通文件时稳定 exit 5；报告 v2 含模型/seed/adapter SHA-256/环境/replay 校验。
- Manual：`--algorithm dfs` 生效、BFS/tour 限额生效、guard 三处（生成/执行/harness）统一求值、嵌套父状态 harness 展开叶状态匹配、未知 guard 变量显式报错、测试脚本无吞错（sabotage 试验证明失败会传播）、统一 oracle 在预算内完成（见第 3 节）。
- Plan/Goal：失败/超时/截断/解析/模型/写失败退出码全谱实测（4/5/2/3/1）；direct/standalone 遇带参流程显式拒绝而非静默零参调用；严格 JSON 解析（12 个协议变体实测）；参数缺来源/多来源/类型不匹配在模型期拒绝；max-flows 全局预算；adapter 超时按进程组清理（实测孙进程无残留）。

## 3. 统一隐藏问题 oracle（Q1–Q6）重跑结果

冻结资产与预算同 review-3（10s/进程、24 run/项目）。本轮关键改进：四个项目都把 oracle 内嵌为产品回归（OMX `make test` 内 84-run 全矩阵、LazyCodex CTest `evaluation_q1..q6`、Manual `make oracle`、Plan/Goal `make check` 内 24-run），且内嵌模型与冻结资产逐字节/语义一致（均经 diff 验证）。Plan/Goal 本轮额外按两种口径运行（项目内嵌口径含 `--no-function-flows`；review-3 冻结口径为默认模式含函数流程）：

| 方案 | clean 对照 | mutant 判错 | 缺陷发现率 | 正常误报率 | mutant 工具退出码 | 状态桥/适配披露 |
|---|---:|---:|---:|---:|---|---|
| OMX | 12/12 | 12/12 | 6/6 = 100% | 0/12 | 1（断言失败） | 无需状态桥（进程内回调 + snapshot/restore） |
| LazyCodex | 12/12 | 12/12 | 6/6 = 100% | 0/12 | 4（mismatch） | 每步新进程 + 私有状态文件（评审适配层） |
| Manual | 12/12 | 12/12 | 6/6 = 100% | 0/12 | 1（harness 断言失败） | 无需状态桥（单进程内 SUT + 影子变量）；另有 native-q2 6/6 |
| Plan/Goal（内嵌口径） | 12/12 | 12/12 | 6/6 = 100% | 0/12 | 4 | adapter 状态文件（评审适配层） |
| Plan/Goal（冻结口径） | 2/12 | 12/12 | 6/6 = 100% | **10/12（83%）** | 4（含 clean） | 同上 |

Manual 的 review-3"统一 oracle 未在预算内完成"已解除：内嵌 oracle 全程 2.7s（约 90ms/run），独立抽检确认 mutant 在真实不匹配点失败、断言由 DSL 编译进 harness 而非脚本/适配层代写。

**Plan/Goal 冻结口径 83% 误报（新发现 N1）经汇总者独立复现**：Q2 clean（MUTANT=0）exit 4、45/55 通过；55 条失败流程全部含 `q_pop_first`，示例 `q_open,q_open,q_push1,q_push2,q_push2,q_pop_first,q_pop_first` 报 `return mismatch for q_pop_first: expected 11, got 22`——新生成的重复调用流程与"按函数名单值 expect"判定语义不兼容（262/270 失败由此触发，8 条为跨流程状态残留）。SUT 行为正确，失败全部是工具判定语义造成；项目内嵌 oracle 用 `--no-function-flows` 规避且 README 未对默认模式警告。按通用标准 §7"High = 错误结果被判为成功"，误报不满足 High 字面定义，定为 **Medium（偏高）**，但列为 Plan/Goal 发布阶段门第一条件项。冻结口径在本轮 HEAD 上的完整重跑结果（含每个 run 的退出码与判定）保存在 [wise_combine_test-review-4-assets/plan_goal/results-frozen-protocol](wise_combine_test-review-4-assets/plan_goal/results-frozen-protocol/)；review-3-assets 的原始冻结产物已恢复原状、仍对应 review-3 版本。

生成 probe（与供应触发分开测量）结论：

| 能力 | OMX | LazyCodex | Manual | Plan/Goal |
|---|---|---|---|---|
| 非自环重走 | 原生（含二环有界） | 原生 | 原生 | 原生 |
| 空流程 | 原生（退化空集） | 原生 | 不支持（未在矩阵分类） | 原生（空转 passed） |
| 子集 | 非目标（全 call 拓扑） | 不生成（未归类，Low） | 原生 | 原生 |
| 重复调用 | 需唯一 ID 别名（声明边界） | 原生（自环 9 条不同长度） | 原生 | 原生（`--max-function-repeats`） |
| 限额生效 | 原生 + 截断非零退出 | 原生（exit 3） | 原生（tour 有 truncated 报告） | 原生（全局预算，exit 5） |
| seed | 原生（同 seed 复现/异 seed 差异，selection 入 trace） | 原生 | 原生 | 无效（仅记录不影响生成，生成完全确定） |
| 自动发现触发序列 | 不适用（非目标） | 未测 | 6/6 无 cap；100-case 冻结预算下 4/6（枚举顺序依赖，如实披露） | 未证明（触发序列由模型供应） |

## 4. 执行模式对齐表

| 模式 | OMX | LazyCodex | Manual | Plan/Goal |
|---|---|---|---|---|
| 进程内/direct | 状态场景子进程 + snapshot/restore + 事务回滚 | —（无进程内模式，声明边界） | 直接链接/dylib | dlopen 子进程；带参/expect_output 流程显式拒绝（声明边界） |
| 外部 adapter | —（relation 模式按 C 契约进程隔离） | 唯一模式；严格 RFC8259、allowlist、双超时、输出上限、固定环境 | — | 主路径；真实传参、进程组清理、协议严格解析 |
| 生成 harness/standalone | — | — | harness（C/C++/dylib，子进程+进程组+超时+崩溃非零退出；无输出上限） | standalone；带参显式拒绝（声明边界） |
| 状态跨步持久化 | snapshot/restore + 失败回滚（文档化） | 明确外部状态（评审适配层用状态桥） | 单进程内影子变量（native） | adapter 状态文件（评审适配层） |
| 失败退出码 | 0/1（fail-closed） | 0/2/3/4/5 细分 | 0/1/2 | 0–5 全谱（本轮实测） |

## 5. 新发现问题汇总与硬门槛

本轮四个项目共新发现 **0 High、7 Medium、约 20 Low**（明细见各独立报告第 6 节）：

| 方案 | Medium | 关键 Low |
|---|---|---|
| OMX | M1 超时只杀直接子进程，孙进程残留（违反专项 §2.3 进程组清理） | CLI 失败摘要缺 expected/actual；allocation-failure/null-report 注入缺失；improvement-plan 头部过期；定时 AI 全权限无人值守 |
| LazyCodex | 无 | 不可达旧解析分支；SIGPIPE 理论竞态（20 次未复现）；`.agents` 追踪矛盾（含凭证，Low-Medium）；子集能力未归类；覆盖率口径漂移 |
| Manual | N1 `--sequence` 不校验资源参数来源（响亮编译错误，非误判）；N2 harness 无输出上限/SIGPIPE；N3 设计/ADR 未按自身治理规则同步 | 重复 transition 静默接受；`--sequence` 对状态机模型静默忽略；空流程未归类；oracle 中间产物不留存；AI 资料版本绑定陈旧 |
| Plan/Goal | N1 默认模式对带 expect 有状态 SUT 系统性误报（冻结口径 10/12，README 未警告）；N2 value 约束不满足时全流程误杀且无诊断 | 截断警告破坏 JSON 合法性；replay 不校验 model_digest；direct 报错指引矛盾；超时硬编码；seed 无效但 CLI 描述误导 |

硬门槛核对（通用标准 §8.2）：四个项目均无未缓解 High；核心需求通过；可靠性关键场景（超时/崩溃/mutant 判错/退出码）已验证；oracle 独立冻结且证据覆盖率 94–96%（≥60%）；测试脚本无吞失败。**无分数封顶，无阶段门硬阻断。**

## 6. 软件分（专项七项平均）与横向排名

| 维度 | OMX | LazyCodex | Manual | Plan/Goal |
|---|---:|---:|---:|---:|
| ① 模型表达和契约 | 9.0 | 8.0 | 7.5 | 7.5 |
| ② 组合生成正确性和覆盖 | 8.0 | 7.0 | 7.0 | 7.0 |
| ③ 执行可靠性、隔离与安全 | 7.5 | 8.0 | 7.0 | 7.0 |
| ④ 报告、诊断和复现 | 8.0 | 8.0 | 6.5 | 7.5 |
| ⑤ 工具自身测试完备性 | 8.5 | 8.0 | 7.0 | 7.5 |
| ⑥ 设计、文档和后续维护 | 9.0 | 7.5 | 6.5 | 7.5 |
| ⑦ 项目计划、风险和流程治理 | 9.0 | 7.5 | 5.5 | 7.0 |
| **S_software** | **8.4** | **7.7** | **6.7** | **7.3** |
| review-3 | 8.4 | 6.2 | 4.4 | 6.3 |

软件分排名：**OMX > LazyCodex > Plan/Goal > Manual**。LazyCodex（+1.5）与 Manual（+2.3）升幅最大：前者的全部 High/Medium 关闭且测试从 19 增至 45（Debug+ASan 双绿、覆盖率门禁 84%）；后者解除了统一 oracle 未完成与隔离/guard 阻断。Plan/Goal +1.0：四项 High 全部修复，但新引入的默认模式误报（N1）与 value 约束误杀（N2）压住了 ②③。OMX 持平 8.4：③因新发现的进程组清理缺口下调、⑤因 oracle 内嵌与覆盖归集修复上调。

## 7. AI 附件补充评价

启用原因与 review-3 相同（四项目均为 AI 编排交付且比较目标含 AI 可延续性）。本轮重点核验补救期内新增 AI 台账的真实性：

| 方案 | 贡献效果 | 可延续资料 | 接口能力 | 质量与安全 | 成本与持续性 | S_AI | S_final = 0.85×软件 + 0.15×AI |
|---|---:|---:|---:|---:|---:|---:|---:|
| OMX | 7.0 | 8.0 | 3.5 | 6.0 | 4.5 | **5.8** | **8.04** |
| LazyCodex | 5.0 | 7.0 | 2.5 | 6.0 | 3.0 | **4.7** | **7.25** |
| Manual | 5.0 | 7.0 | 2.0 | 4.0 | 3.0 | **4.2** | **6.33** |
| Plan/Goal | 5.0 | 7.5 | 2.0 | 4.5 | 3.5 | **4.5** | **6.87** |

- OMX 是唯一留下**哈希可验证 AI provenance** 的方案：`.omx/ai/runs.jsonl` 记录的输入资产 SHA256 与实际文件 3/3 吻合，证明 09-14 的 AI 维护运行真实消费了 review-3 资产并驱动修复；但单运行成本为 null、无人工批准者身份、定时任务以 full-access 无人值守运行（L4）。
- LazyCodex 的 evidence 链（blocker-red 先失败证据、gate-review SHA 绑定）经抽查真实，但无模型名/版本/成本（附件封顶 5 分实际生效）；`.agents` 追踪矛盾仍未解。
- Manual 的"AI 续开发演练"即本轮被验证的补救本身（非模板文字）；AI-COST 诚实标注"未建立"，无虚报。
- Plan/Goal 的 ai-usage-log 条目与实际提交一一对应、验证命令可复跑；自登记 D-006（model_known/cost_known 全 false），不宣称净收益。
- 共同缺口不变：均无带来源/权限/版本语义的 AI 专用查询接口（封顶低位），无供应商替代/离线复现记录；AI 分不能抵消软件问题。

## 8. 最终排名与阶段门

| 排名 | 方案 | 软件分 | AI 分 | 最终分 | 发布阶段门 |
|---:|---|---:|---:|---:|---|
| 1 | OMX | 8.4 | 5.8 | **8.04** | 有条件通过（条件：M1 进程组清理 + allocation-failure/null-report 注入后刷新证据） |
| 2 | LazyCodex | 7.7 | 4.7 | **7.25** | 有条件通过（条件：`.agents` 矛盾、N1/N2 清理、发布回滚记录） |
| 3 | Plan/Goal | 7.3 | 4.5 | **6.87** | 有条件通过（条件：N1 默认模式语义修复或文档化警告、N2 修复；若 N1 升 High 则暂缓） |
| 4 | Manual | 6.7 | 4.2 | **6.33** | 有条件通过（条件：N1 资源来源校验、N2 输出上限、N3 设计同步） |

LazyCodex 与 Plan/Goal 最终分差 0.38 < 0.5，按通用标准 §10 依次比较：证据覆盖率（94.9% vs 96%，不足以区分）→ 软件分（7.7 vs 7.3）→ 残余风险（LazyCodex 无 Medium 仅 Low；Plan/Goal 有 N1 默认模式系统性误报 + N2 静默全杀）。结论 **LazyCodex 排在 Plan/Goal 之前**，与 review-3 的风险排序方向一致但本轮不再需要"风险否决均分"——软件分本身已反超。

其余阶段门（需求/设计/编码/测试/维护）明细见各独立报告第 9 节；要点：四个方案的需求、编码阶段门全部通过（review-3 时均各有条件），测试与发布阶段门全部为有条件通过。**这是四轮评审以来首次没有方案处于"发布不通过/暂缓"状态，但也没有方案获得无条件通过。**

## 9. 与历史各轮评审分数的对比

跨轮对比先声明可比性口径（依通用标准 §8.1"不同阶段的总分不可直接排名"）：

- **第一轮（review-1，早期记录）**：三方案对比（无 Manual），无量化打分，仅定性排序，不参与分数对比；
- **第二轮（review-2）**：五维评分（目标匹配/灵活全面/可靠性/测试完备/AI 管理可维护，10 分制平均），维度构成与第三、四轮的专项七维不同，综合分只作趋势参考，数值不可与 R3/R4 直接比较；
- **第三轮（review-3）起**：冻结为专项七项软件分 + AI 五项分，最终分 = 0.85×软件 + 0.15×AI。本轮沿用同一口径，逐维分数可直接比较。

### 9.1 软件分逐维对比（review-3 → review-4）

| 维度 | OMX | LazyCodex | Manual | Plan/Goal |
|---|---|---|---|---|
| ① 模型表达和契约 | 9.0 → 9.0 | 6.0 → **8.0** | 6.0 → **7.5** | 7.0 → **7.5** |
| ② 组合生成正确性和覆盖 | 8.0 → 8.0 | 6.0 → **7.0** | 5.0 → **7.0** | 6.5 → **7.0** |
| ③ 执行可靠性、隔离与安全 | 8.0 → **7.5** | 6.0 → **8.0** | 3.0 → **7.0** | 5.0 → **7.0** |
| ④ 报告、诊断和复现 | 8.0 → 8.0 | 5.5 → **8.0** | 4.0 → **6.5** | 5.5 → **7.5** |
| ⑤ 工具自身测试完备性 | 8.0 → **8.5** | 7.0 → **8.0** | 3.0 → **7.0** | 7.0 → **7.5** |
| ⑥ 设计、文档和后续维护 | 9.0 → 9.0 | 6.5 → **7.5** | 6.0 → **6.5** | 7.0 → **7.5** |
| ⑦ 项目计划、风险和流程治理 | 9.0 → 9.0 | 6.5 → **7.5** | 4.0 → **5.5** | 6.0 → **7.0** |
| **S_software** | **8.4 → 8.4（持平）** | **6.2 → 7.7（+1.5）** | **4.4 → 6.7（+2.3）** | **6.3 → 7.3（+1.0）** |

除 OMX 的 ③（本轮新发现的进程组清理缺口）外，四个方案没有任何维度下调：LazyCodex 七维全升，Manual 七维全升（③⑤ 各 +4.0 为最大单维升幅），OMX ③↓⑤↑ 相抵——总分持平，但质量重心从输入边界移到隔离完整性。

### 9.2 AI 分与最终分对比（review-3 → review-4）

| 方案 | S_AI | S_final |
|---|---|---|
| OMX | 5.0 → **5.8** | 7.89 → **8.04** |
| LazyCodex | 4.3 → **4.7** | 5.92 → **7.25** |
| Manual | 3.6 → **4.2** | 4.28 → **6.33** |
| Plan/Goal | 4.4 → **4.5** | 6.02 → **6.87** |

AI 分普升的共同原因是补救轮首次留下了可验证的 AI 台账/演练证据（尤其 OMX 的哈希 provenance，5.0→5.8）；但四方案仍均未达到"可证 AI 净收益"的证据门槛（模型版本/成本/人工基线缺失，附件封顶规则继续生效），AI 分差异不构成排名的主导因素。

### 9.3 四轮排名演变

| 方案 | R1（早期，定性，三方案） | R2（五维综合，趋势参考） | R3（软件分 / 最终分） | R4（软件分 / 最终分） |
|---|---|---|---|---|
| OMX | 3/3（"最小可用库 + 自认未完成的骨架"） | **1**/4（8.8） | **1**/4（8.4 / 7.89） | **1**/4（8.4 / 8.04） |
| LazyCodex | **1**/3（"最接近可交付的产品"） | **2**/4（7.7） | 数值 3/4（6.2 / 5.92），风险排序 2 | **2**/4（7.7 / 7.25） |
| Plan/Goal | 2/3（"功能面较全的原型"） | 3/4（6.9） | 数值 **2**/4（6.3 / 6.02），风险排序 3 | 3/4（7.3 / 6.87） |
| Manual | 未参评 | 4/4（6.6） | 4/4（4.4 / 4.28） | 4/4（6.7 / 6.33） |

趋势解读：

- **OMX**：自 R2 起连续三个计分轮次第一，软件分稳定 8.4。R1 的末位定性结论反映的是早期快照状态——R1→R2 之间其补齐了 trace/replay、sanitizer 和迭代证据链。这说明跨轮分差主要度量"评审驱动补救后的交付状态"，不是方案的固定属性。
- **LazyCodex**：唯一经历"定性第一 → 数值第三 → 反超回第二"的方案。R3 时其软件分被参数契约/报告缺口压低，但风险排序已高于 Plan/Goal；R4 数值排序与风险排序首次一致（软件分 7.7 > 7.3，与 R2 的 7.7 属不同口径的数值巧合，不可比较）。
- **Manual**：R3 谷底（4.4，发布不通过）后单轮 +2.3，为四方案跨轮历史最大升幅；排名仍第四，主要受 ⑦治理（5.5）与 ④报告复现（6.5）拖累。
- **Plan/Goal**：R3 数值第二 → R4 第三，是"分数上升、排名下降"的典型：自身 +1.0（四项 High 全部修复）但被 LazyCodex 反超，且新引入的 N1 默认模式误报压住 ②③ 维度。
- **发布阶段门**：R3 无一方案可发布（OMX/LazyCodex/Plan/Goal 暂缓、Manual 不通过）→ R4 四方案均有条件通过，各方案发布条件清单见第 11 节。

## 10. 未支持、未验证与能力边界

按项目自身契约分类（核心契约/已声明可选扩展/明确非目标），本轮核验边界声明与实际行为一致，未发现"把未修缺陷伪装成非目标"的情况（Manual 与 Plan/Goal 的边界文档均写于修复之后，经 git 历史核对）：

- OMX：子集/穷举组合、重复调用展开（需别名）、guard/mutex/parallel 为明确非目标；allocation-failure/null-report 注入未验证。
- LazyCodex：guard/mutex/parallel/count/value/负向流程按 ADR 0004 冻结为非目标；子集/前缀生成未归类（Low）；返回值 expected 断言部分支持（ADR 0005）；fuzz 与发布回滚未验证。
- Manual：经典 t-way 参数组合/GUI/分布式为非目标；空流程不支持但未在矩阵分类；fuzz/sanitizer/覆盖率量化未验证；自动发现 probe 的冻结预算口径需并列披露（无 cap 6/6、100-cap 下 4/6）。
- Plan/Goal：direct/standalone 带参执行为声明边界（D-005 open/future）；随机策略、并发执行为非目标；value 约束 producer 链未支持（文档已写，但 N2 误杀行为未文档化）；生成器自动发现 mutant 触发序列未证明。

## 11. 共同补救与修复优先级

按发布阻断程度排序：

1. **Plan/Goal**：修复 N1（重复调用与单值 expect 的判定语义：按调用序号/状态化 expected，或负向标注机制）并在 README 警告默认模式行为；修复 N2（value 约束只过滤含该函数的流程 + 无诊断问题）。
2. **OMX**：M1 超时按进程组清理后代；补 allocation-failure/null-report 注入回归；刷新 improvement-plan 头部状态。
3. **Manual**：`--sequence` 资源来源模型期校验；harness 输出上限与 SIGPIPE；按 governance 规则同步 design/ADR；oracle 中间产物留存。
4. **LazyCodex**：解决 `.agents` 追踪矛盾（区分凭证与指导文件）；子集能力归类；清理不可达解析分支；SIGPIPE 显式忽略。
5. **共同**：记录补救轮的人工时间与 AI token 成本（四个项目均缺）；覆盖分母定义与 branch-taken 口径统一报告；D-001（Plan/Goal 分支覆盖 80%，期限 09-19）按期跟踪。

## 12. 经验反馈与标准改进建议

- **生成能力扩展必须同步重验判定语义**：Plan/Goal N1 表明，把"每函数一次排列"扩展为"空/子集/重复"后，原有的单值 expect 立即变成误报源。建议在专项标准 §2.2 增加检查项："生成策略变更后，clean 对照必须零误报，且 expected 语义与调用次数/状态兼容"（按通用标准 §11 登记为标准改进候选，版本更新后对所有比较对象同规则补查）。
- **双口径 oracle 有独立价值**：项目内嵌口径（`--no-function-flows`）与冻结默认口径的差集恰好暴露了 N1；单口径复跑会漏掉。
- **哈希绑定的 AI provenance 可验证且成本低**：OMX 的 runs.jsonl（输入 SHA256 + 输出 + 采纳状态）是四方案中唯一能机器验证"AI 真实消费了上轮评审资产"的机制，值得推广为 AI 台账最低标准。
- **短时补救质量可以很高但不留成本痕迹**：Manual 77 分钟、Plan/Goal 87 分钟完成全部 High/Medium 修复，质量经实测确认；但两项目均无时间/token 记录，AI 编排的效率结论仍不可证（与 review-3 结论一致）。
- 本轮再次确认：供应触发 mutant 只证明执行判定链；四个方案中仍只有 Manual 实测了生成器自动发现触发序列（且冻结预算下 4/6，受枚举顺序影响）。

## 13. 下次复评条件

完成第 11 节各自修复优先级中的条目并刷新证据；Plan/Goal 重跑双口径 24-run（冻结口径 clean 误报须降为 0 或有文档化负向标注）；OMX 提供 M1 进程组清理证据；补齐人工时间/AI 成本台账后，按 AI 附件复评 S_AI；如采纳标准改进建议（生成变更后 clean 零误报门禁），对所有方案按新规则补查一次生成 probe。

## 14. 限制声明

本轮结论仅针对上表所列 commit 的可观察交付；AI 编排方式的比较限于本次交付，模型、人工决策、预算和迭代次数未受控，不据此证明某种编排工具普遍更优。Manual 自动发现 probe 的 100-cap 口径与无 cap 口径并列披露，未合并为单一数字。Plan/Goal 冻结口径误报的定级（Medium 偏高）由汇总者裁定，若将其冻结为默认核心契约应升级为 High 并重开阶段门。
